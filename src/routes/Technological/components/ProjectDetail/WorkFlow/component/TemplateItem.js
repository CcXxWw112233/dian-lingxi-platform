import React, { Component } from 'react'
import indexStyles from '../index.less'

export default class TemplateItem extends Component {
  render() {
    return (
      <div className={indexStyles.tempItemContent}>
        
      </div>
    )
  }
}

// 每一个模板选项结构
TemplateItem.defaultProps = {
  
}
